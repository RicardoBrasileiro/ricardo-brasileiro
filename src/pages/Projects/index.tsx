import React from 'react';
import { IRoute } from '../../routes';

const Projects = (props: IRoute): JSX.Element => {
  return (
    <div>
      <p>Início</p>
    </div>
  );
};

export default Projects;
