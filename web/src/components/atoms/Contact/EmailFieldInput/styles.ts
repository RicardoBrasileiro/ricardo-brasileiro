import styled from "styled-components";

export const EmailFieldInputContainer = styled.input`
  width: 100%;

  background: none;
`;

export const EmailFieldInputTextarea = styled.textarea`
  width: 100%;
  min-width: calc(100% - 30px);
  max-width: calc(100% - 30px);
  height: 140px;
  min-height: 24px;
  max-height: 500px;

  background: none;
`;
